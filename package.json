{
  "name": "benedictus",
  "version": "1.0.0",
  "description": "Hello World!",
  "main": "dist/main/main.js",
  "scripts": {
    "build:main": "tsc",
    "build:renderer": "vite build",
    "clean": "rm -rf dist",
    "build": "bun run clean && bun run build:main && bun run build:renderer",
    "start": "bun run build && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jane Doe",
  "license": "MIT",
  "devDependencies": {
    "@types/electron": "^1.6.12",
    "@types/node": "^25.0.6",
    "@vitejs/plugin-vue": "^6.0.3",
    "electron": "^39.2.7",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "lucide-vue-next": "^0.562.0",
    "vue": "^3.5.26"
  }
}